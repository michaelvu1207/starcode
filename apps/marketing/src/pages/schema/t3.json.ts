// Existing t3.json files have this URL committed in their `$schema` field, so
// the legacy route must remain an exact alias of the canonical schema handler.
export { GET } from "./starcode.json.ts";
